# Changesets

Every change to a published package needs a changeset: `pnpm changeset`,
pick the package and the bump, and write what someone upgrading needs to
know. The text lands verbatim in the package's CHANGELOG.
