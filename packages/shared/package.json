{
	"name": "@eisbachcode/emdash-plugin-shared",
	"version": "0.0.0",
	"private": true,
	"description": "Internal helpers shared by the Eisbachcode EmDash plugins. Never published — the plugin build inlines it (external: []), so consumers of a published plugin never resolve this package.",
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "vitest run",
		"typecheck": "tsc --noEmit"
	},
	"peerDependencies": {
		"emdash": ">=0.36.0 <1.0.0"
	},
	"devDependencies": {
		"emdash": ">=0.36.0 <1.0.0",
		"typescript": "^5.9.0",
		"vitest": "^4.1.0"
	}
}
